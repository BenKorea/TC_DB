project:
  type: website
  output-dir: docs

website:
  title: "RAI"
  navbar:
    left:
      - href: index.qmd
        text: Home

format:
  html:
    theme: cosmo
    css: styles.css
    toc: true

editor: visual

execute:
  freeze: auto  # re-render only when source changes